# Cloudflare Pages _redirects
# Format: /old-path  /new-path  301
# Note: Static redirects have been migrated to Cloudflare Bulk Redirects.
# Only dynamic (wildcard) rules remain here.

/categories/:splat  /ja/categories/:splat  301
/tags/:splat  /ja/tags/:splat  301
