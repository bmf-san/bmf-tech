---
title: Building and Pushing Docker Images with GoReleaser
slug: goreleaser-docker-image-build-push
date: 2024-05-29T00:00:00Z
author: bmf-san
categories:
  - Application
tags:
  - Golang
  - GitHub Actions
translation_key: goreleaser-docker-image-build-push
---

Previously, I wrote an article titled [Distributing Go Applications Using GoReleaser](https://bmf-tech.com/posts/GoReleaser%E3%82%92%E4%BD%BF%E3%81%A3%E3%81%A6%E3%81%AEGo%E3%81%AE%E3%82%A2%E3%83%97%E3%83%AA%E3%82%B1%E3%83%BC%E3%82%B7%E3%83%A7%E3%83%B3%E3%82%92%E9%85%8D%E5%B8%83%E3%81%99%E3%82%8B) about how to distribute binaries using GoReleaser, but I also tried pushing images to Dockerhub, so I’ll summarize that here.

For the overall source code, refer to [bmf-san/gondola](https://github.com/bmf-san/gondola).

# Configuring .goreleaser.yaml
```yaml
ockers:
  - image_templates:
      - bmfsan/gondola:latest-amd64
      - bmfsan/gondola:{{ .Version }}-amd64
    use: buildx
    goos: linux
    goarch: amd64
    build_flag_templates:
      - --platform=linux/amd64
  - image_templates:
      - bmfsan/gondola:latest-arm64
      - bmfsan/gondola:{{ .Version }}-arm64
    use: buildx
    goos: linux
    goarch: arm64
    build_flag_templates:
      - --platform=linux/arm64
  - image_templates:
      - bmfsan/gondola:latest-arm
      - bmfsan/gondola:{{ .Version }}-arm
    use: buildx
    goos: linux
    goarch: arm
    build_flag_templates:
      - --platform=linux/arm
```

It looks a bit lengthy, but this is how you write the options for dockers.

The length is due to adjusting to create artifacts in the same way as the options for builds (binary creation). (It might be possible to write it more concisely...)

```yaml
builds:
  - env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm
      - arm64
    main: ./cmd/main.go
```

Refer to the default values of goreleaser below.

cf. [github.com/goreleaser/goreleaser/blob/main/.goreleaser.yaml](https://github.com/goreleaser/goreleaser/blob/main/.goreleaser.yaml)

# Creating Dockerfile
```
FROM gcr.io/distroless/static-debian12
COPY gondola /
ENTRYPOINT ["./gondola", "-config", "config.yaml"]
```

The binary being copied is generated by goreleaser, so there’s no need to build it yourself.

# Editing GitHub Actions Workflow
```yaml
name: GoReleaser

on:
  push:
    tags:
      - '*'

permissions:
  contents: write

jobs:
  goreleaser:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        go-version: [ '1.22.3' ]
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ matrix.go-version }}
      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - name: Run GoReleaser
        uses: goreleaser/goreleaser-action@v5
        with:
          distribution: goreleaser
          version: latest
          args: release --clean
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
```

Since I want to push the image to Dockerhub, I adjusted it to log in to Dockerhub.

Make sure to set the secrets in the GitHub repository settings in advance.

And that's it! It's easy to set up! GoReleaser is convenient..!!